# Guimp

A lightweight image editor written in C using SDL2.

This project aims to recreate a simplified version of GIMP with custom UI rendering and basic image manipulation.

---

## Dependencies

sudo apt install libsdl2-dev libsdl2-image-dev libsdl2-ttf-dev


## launching

make && ./guimp